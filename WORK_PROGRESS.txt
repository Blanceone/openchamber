OpenChamber 本机桌面裁剪 — 工作进展与后续事项
============================================
日期：2026-08-06
分支：dev
远程：origin = https://github.com/Blanceone/openchamber.git
上游：upstream = https://github.com/openchamber/openchamber.git
最近提交范围：阶段 C–E（相对 83ec399d 本机桌面裁剪提交）

路径约束（必须遵守）
--------------------
1. 依赖库 / 下载缓存 / 安装包缓存：只允许放到 D:\work\ai\openchamber\depends（相对仓库 ../depends）
2. 编译工具链 / SDK / native build helpers：只允许放到或安装在 D:\work\ai\builder_tools
   （已写入 .cursor/rules/local-windows-paths.mdc 与 AGENTS.md）

一、产品目标（已确认）
--------------------
- 仅保留 Windows Electron 桌面产品面（packages/electron + web + ui）
- 本机工作为主，需要本地 Git / GitHub（含 Git SSH 身份）
- 不再提供 OpenChamber 远程主机、Private Relay、外网隧道、配对扫码连入、LAN 暴露等产品入口

二、已完成工作
--------------

【阶段 A】Windows 桌面产品裁剪（已入库：83ec399d）
- 删除 packages/vscode、packages/mobile、packages/docs；CI/release 收敛 Windows
- Windows 环境辅助：bunfig.toml、windows-electron-env.ps1、repair-windows-electron.ps1

【阶段 B】UI/Electron/CLI 去掉远程产品入口（已入库：83ec399d）
- Settings/Header/深链去掉 Remote/Tunnel/SSH 主机切换
- CLI tunnel / connect-url 入口禁用；保留 Settings → Git / GitHub

【阶段 C】关闭服务端远程攻击面 + 文案清理
- local-desktop-remote.js：pairing/connect/APNs → 410；disabled tunnel/relay stubs
- Electron 强制 loopback，忽略 desktopLanAccessEnabled
- CLI 帮助去掉 tunnel/connect-url/LAN/relay 广告
- 各 locale 删除 remoteInstances/tunnel 孤儿 i18n 键（约 3685 条）
- DesktopNetworkSettings 评估：暂不新增独立锁屏密码 UI；settings/env 仍可选用

【阶段 D】runtime-transport 迁移
- 新增 packages/ui/src/lib/runtime-transport/（socket / WS wrapper / transport-error / relay stub）
- 删除 packages/ui/src/lib/relay/** 私有加密与隧道客户端
- 生产调用点改指向 runtime-transport

【阶段 E】死分支卸载 + boot 瘦身 + 冒烟
- runtime-fetch / runtime-auth：去掉私有 relay 分支（始终直连）
- boot：pairing + managed-tunnel 用 disabled stubs；显式挂载 tunnel/relay 410（避免落入 OpenCode 代理超时）
- CLI：不再 import commands-tunnel / cli-tunnel-profiles；capabilities 改为静态常量
- 删除 .agents/skills/serve-sim 墓碑技能

三、验证
--------
已跑：
- bun test：local-desktop-remote、runtime-switch、runtime-fetch、runtime-auth — 通过
- bun test cli：fallback capabilities + tunnel disabled — 通过
- node --check：server/index.js、cli.js、local-desktop-remote.js
- electron:dev 部分冒烟：
  - Electron 启动，本机服务 127.0.0.1:57123
  - /api/client-auth/*、/connect、POST /api/push/apns-token → 410
  - OpenCode CLI 未安装（spawn opencode ENOENT）— 环境问题
  - 冒烟结束后残留进程已清理

未跑 / 未证明：
- 完整会话 / 终端 / Git 页（需本机 OpenCode CLI）
- electron:build 可分发安装包

四、刻意未做 / 后续
------------------
优先级高：
1. 安装 OpenCode CLI 后完整会话/终端/Git 冒烟
2. 具备 VS Build Tools（装于 D:\work\ai\builder_tools）时跑 electron:build
3. 物理删除 tunnels 实现目录（保留 settings 仍用的 types）与大部分 relay/**（保留 signing-key 直至 APNs 彻底去掉）

优先级中：
4. 删除 commands-tunnel.js / commands-connect-url.js / cli-tunnel-profiles.js 等实现文件
5. VS Code / Capacitor 运行时分支大扫除
6. 评估独立「应用锁屏密码」设置（不绑定 LAN）

优先级低：
7. CONTRIBUTING 写入 Windows 打包注意点与 depends/builder_tools 路径约束
8. 安装包签名（若外部分发需要）

五、冒烟清单
------------
[x] electron:dev 能启动（本机服务 up；OpenCode 因 PATH 缺失未起）
[ ] 设置无 Remote / Tunnel；Git 可用；会话正常；终端可用（需 OpenCode）
[x] CLI tunnel/connect-url → disabled（测试覆盖）
[x] 远程 API → client-auth/connect/apns 410；tunnel/relay 显式 stub
