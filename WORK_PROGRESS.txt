OpenChamber 本机桌面裁剪 — 工作进展与后续事项
============================================
日期：2026-08-08（续：阶段 G）
分支：dev
远程：origin = https://github.com/Blanceone/openchamber.git
上游：upstream = https://github.com/openchamber/openchamber.git
最近完成：阶段 A–F 已入库；阶段 G 为本机工作区完成项（含 electron:build 复核）
本机安装包产物（本次重建，未签名）：
  packages/electron/dist/OpenChamber-1.18.1-win-x64.exe（约 158.5 MB，2026-08-08）

路径约束（必须遵守）
--------------------
1. 依赖库 / 下载缓存 / 安装包缓存：只允许放到 D:\work\ai\openchamber\depends（相对仓库 ../depends）
2. 编译工具链 / SDK / native build helpers：只允许放到或安装在 D:\work\ai\builder_tools
   （已写入 .cursor/rules/local-windows-paths.mdc、AGENTS.md、CONTRIBUTING.md）
3. 例外（Microsoft 强制路径）：Windows Kits 头文件/库仍安装到
   C:\Program Files (x86)\Windows Kits\10（VS Build Tools 本身在 builder_tools\vs2022）

一、产品目标（已确认）
--------------------
- 仅保留 Windows Electron 桌面产品面（packages/electron + web + ui）
- 本机工作为主，需要本地 Git / GitHub（含 Git SSH 身份）
- 不再提供 OpenChamber 远程主机、Private Relay、外网隧道、配对扫码连入、LAN 暴露等产品入口

二、已完成工作
--------------

【阶段 A】Windows 桌面产品裁剪（已入库：83ec399d）
- 删除 packages/vscode、packages/mobile、packages/docs；CI/release 收敛 Windows
- Windows 环境辅助：bunfig.toml、windows-electron-env.ps1、repair-windows-electron.ps1

【阶段 B】UI/Electron/CLI 去掉远程产品入口（已入库：83ec399d）
- Settings/Header/深链去掉 Remote/Tunnel/SSH 主机切换
- CLI tunnel / connect-url 入口禁用；保留 Settings → Git / GitHub

【阶段 C】关闭服务端远程攻击面 + 文案清理（已入库：c0e88555）
- local-desktop-remote.js：pairing/connect/APNs → 410；disabled tunnel/relay stubs
- Electron 强制 loopback，忽略 desktopLanAccessEnabled
- CLI 帮助去掉 tunnel/connect-url/LAN/relay 广告
- 各 locale 删除 remoteInstances/tunnel 孤儿 i18n 键（约 3685 条）
- DesktopNetworkSettings 评估：暂不新增独立锁屏密码 UI；settings/env 仍可选用

【阶段 D】runtime-transport 迁移（已入库：c0e88555）
- 新增 packages/ui/src/lib/runtime-transport/
- 删除 packages/ui/src/lib/relay/** 私有加密与隧道客户端

【阶段 E】死分支卸载 + boot 瘦身（已入库：c0e88555）
- runtime-fetch / runtime-auth：去掉私有 relay 分支
- boot：pairing + managed-tunnel 用 disabled stubs；显式挂载 tunnel/relay 410
- CLI：capabilities 静态常量；不再激活 tunnel 实现

【阶段 F】OpenCode CLI 安装 + 会话/终端/Git 冒烟（已入库：f2d60695 工作日志）
- OpenCode CLI 1.18.12 + electron:dev API 冒烟（会话/终端/Git；远程 API 410）

【阶段 G】物理删除远程实现 + 工具链 + electron:build（本机 2026-08-08）
代码清理：
- 删除 CLI：commands-tunnel.js、commands-connect-url.js、cli-tunnel-profiles.js、cli-tunnel-utils.js
- 删除 server tunnels 实现（保留 types.js + types.test.js + DOCUMENTATION.md）
- 删除 server relay 实现与 APNs runtime（保留 DOCUMENTATION.md；boot 用 createDisabledApnsRuntime）
- Capacitor 死分支清理；isVSCodeRuntime() 恒 false；runtimeConfig 去掉 desktopRelayRestore
- Windows HMR：packages/web/scripts/run-dev-server.mjs 正确展开 OPENCHAMBER_PORT
- CONTRIBUTING / electron README：路径约束与打包注意点
- Settings：SettingsPageSlug 无 remote/tunnel（代码确认；GUI 点击路径仍未做）
- package.mjs：改用工作区 electron-builder/cli.js（避免 bun x 丢 app-builder-bin）
- prepare-opencode-cli：优先 ../depends/opencode-cli；Windows curl --ssl-no-revoke

工具链 / 缓存（本机）：
- VS Build Tools → D:\work\ai\builder_tools\vs2022
  （Workload.VCTools + Windows11SDK.22621 + VC.Runtimes.*.Spectre）
- OpenCode CLI zip → D:\work\ai\openchamber\depends\opencode-cli\1.18.12\...
- Electron / electron-builder 缓存 → ../depends/electron、../depends/electron-builder
- VS bootstrapper → ../depends/vs-buildtools\vs_BuildTools.exe

三、验证
--------
已跑：
- bun test：local-desktop-remote、runtime-fetch、runtime-auth — 38 pass
- bun test：tunnels/types.test.js — 4 pass
- bun run dead-code（非阻塞报告；无阶段 G 新增阻断项）
- node --check：package.mjs、prepare-opencode-cli.mjs
- electron:build 完整链：web assets → prepare OpenCode CLI → bundle main →
  rebuild native（node-pty）→ NSIS 未签名安装包 — 成功
  产物：packages/electron/dist/OpenChamber-1.18.1-win-x64.exe
        packages/electron/dist/win-unpacked/
        packages/electron/dist/latest.yml

未跑 / 未证明：
- Settings GUI 目视点击确认（代码已无 Remote/Tunnel slug）
- 安装包代码签名（无 CSC；刻意未做）
- 安装包首次安装后的完整 GUI 冒烟（可用既有阶段 F API 冒烟作间接证据）

四、刻意未做 / 后续
------------------
优先级高：无阻塞项（阶段 G 计划项已完成）

优先级中：
1. Settings GUI 目视确认无 Remote / Tunnel 入口
2. 可选：安装包首次启动 GUI 冒烟

优先级低：
3. 安装包签名（若外部分发需要 CSC）
4. 若上游修复 Windows ARM64 OpenCode CLI，去掉 prepare-opencode-cli 的 x64-baseline 临时方案

五、冒烟清单
------------
[x] electron:dev 能启动（阶段 F）
[x] Git API 可用；会话 create/list 正常；终端 create/delete 可用（阶段 F）
[x] CLI tunnel/connect-url → disabled
[x] 远程 API → client-auth/connect/apns/tunnel 410
[x] Settings 代码确认无 Remote / Tunnel 页 slug
[ ] Settings GUI 目视确认（未做点击路径）
[x] electron:build 安装包（2026-08-08 未签名重建成功）
