OpenChamber 本机桌面裁剪 — 工作进展与后续事项
============================================
日期：2026-08-10（阶段 N 落地 + Wiki 树形侧栏）
分支：dev
远程：origin = https://github.com/Blanceone/openchamber.git
上游：upstream = https://github.com/openchamber/openchamber.git
最近提交：见本次变更（基线曾为 8176bec0 Stage N 计划）
本机安装包产物（含 OpenWiki；未签名；跳过 rebuild:native，无 VS Build Tools）：
  packages/electron/dist/OpenChamber-1.18.1-win-x64.exe（2026-08-10 17:15，约 207 MB）
  packages/electron/dist/win-unpacked/（含 resources/openwiki）

路径约束（必须遵守）
--------------------
1. 依赖库 / 下载缓存 / 安装包缓存：只允许放到 D:\work\ai\openchamber\depends（相对仓库 ../depends）
2. 编译工具链 / SDK / native build helpers：只允许放到或安装在 D:\work\ai\builder_tools
   （已写入 .cursor/rules/local-windows-paths.mdc、AGENTS.md、CONTRIBUTING.md）
3. 例外（Microsoft 强制路径）：Windows Kits 头文件/库仍安装到
   C:\Program Files (x86)\Windows Kits\10（VS Build Tools 本身在 builder_tools\vs2022）
4. 本机工作区在 D:\lxh\ai\openchamber-my\openchamber：已用 junction
   D:\lxh\ai\openchamber-my\depends → D:\work\ai\openchamber\depends
   使 package.json / prepare-openwiki 的 ../depends 与路径规则一致

一、产品目标（已确认）
--------------------
- 仅保留 Windows Electron 桌面产品面（packages/electron + web + ui）
- 本机工作为主，需要本地 Git / GitHub（含 Git SSH 身份）
- 不再提供 OpenChamber 远程主机、Private Relay、外网隧道、配对扫码连入、LAN 暴露等产品入口

二、已完成工作
--------------

【阶段 A–M】见历史提交 83ec399d … a3e871e4（Windows 裁剪、远程关闭、OpenWiki 嵌入、网关、zh-CN）

【阶段 N】OpenWiki 格式设置 UX + 参考文档解析 + Word 导出（2026-08-10）
硬约束：不改 openwiki 工程流水线与 agent 架构；仍读写 `.wiki/INSTRUCTIONS.md` + `.wiki/FORMAT.md`。

已实现：
- N1 服务端
  - `.wiki/reference-sources/` 导入/清单/删除（≤10；>10MB 确认；>50MB 拒绝；.md/.doc/.docx）
  - `POST /api/openwiki/format/parse`（job `parse-format`，同 LLM 网关，产草案）
  - `GET /api/openwiki/format/draft`、`POST .../merge`、`POST .../reset`
  - `POST /api/openwiki/export/docx`（本地 MD→DOCX，adm-zip，无新依赖）
  - 默认提示词含简体中文 + Mermaid 规则
- N2 Settings UI
  - 去掉预设主流程；当前提示词编辑 + Reset
  - 参考文档拖放/多选/选文件夹；解析 / 合并 / 草案预览
  - 全 locale + settings search 更新
- N3 主 UI Export Word + Electron
  - Wiki 面 Export Word；选输出目录；每页一个 `.docx`
  - IPC：`desktop_scan_reference_folder` / `desktop_read_user_file` / `desktop_write_files`
- N4 文档与单测
  - `openwiki/DOCUMENTATION.md`、`docs/OPENWIKI_INTEGRATION.md`
  - `reference-sources.test.js` / `format-parse.test.js` / `md-docx.test.js`
  - `stage-n-smoke.mjs`（服务端自动化冒烟 14/14）

【Wiki 树形侧栏】（2026-08-10）
- 问题：主 UI 仅平铺 `.md`（含子目录相对路径字符串），看不出真实文件夹结构
- 修复：`wiki-tree.ts` 递归列举 + `buildWikiTree` 嵌套；`WikiPageTree` 可展开文件夹树
- 侧栏默认展开全部目录；跳过 `reference-sources` 等控制目录
- 单测：`wiki-tree.test.ts` 6/6；已打入 17:15 安装包

【本机环境】
- 已清理旧 AppData：`%APPDATA%\OpenChamber`、`OpenChamber Dev`、electron-updater 缓存
- 本机暂无 VS Build Tools：`electron:build` 全量会卡在 `rebuild:native`；打包改走跳过 native、保留 N-API prebuilds + `rebuild:openwiki-native`

三、验证
--------
已跑：
- vitest：openwiki reference-sources / format-parse / md-docx / format 语言+Mermaid；ui `wiki-tree`
- 阶段 N 服务端冒烟：`bun packages/web/server/lib/openwiki/stage-n-smoke.mjs`（14/14）
- 打包：`verify:openwiki` staged + packaged；安装包 2026-08-10 17:15
- bun run dead-code（新增 WikiPageTree / wiki-tree 等后）

未闭环：
- 完整 Generate/Update 产出稳定中文正文（建议非 free 模型人工确认）
- 阶段 N **GUI** 冒烟（Settings 拖放/选文件夹、真实模型解析合并、Electron 选目录写 docx）
- 安装 VS Build Tools 到 `D:\work\ai\builder_tools\vs2022` 后恢复完整 `rebuild:native`

四、刻意未做 / 后续
------------------
优先级高：
1. 阶段 N GUI 冒烟（导入 / 真实模型解析合并 / Reset / Export Word 写盘）
2. 用已登录非 free 模型再跑完整 Generate + Update，确认中文 .wiki 正文

优先级中：
1. ChatGPT Codex / Copilot enterprise 边缘失败 UX
2. 匿名 Zen / 网络 TLS 稳定性（若仍用免费档）
3. 安装 VS Build Tools（builder_tools）以恢复全量 native rebuild

优先级低：
1. 安装包签名
2. Win ARM64 OpenCode CLI 临时方案清理
3. openwiki 上游升级演练

五、冒烟清单
------------
[x] electron:dev / Git / 会话 / 终端（阶段 F）
[x] CLI tunnel/connect-url disabled；远程 API 410
[x] Settings 无 Remote / Tunnel（代码 + GUI）
[x] 含 OpenWiki 的安装包 + verify:openwiki:packaged（多次重建至 17:15）
[x] OpenWiki JSON 中间件 + preflight/generate 入队
[x] reasoning_content / stream_options 网关修复（源码 + 单测；装入包）
[x] Wiki 语言固定 zh-CN（源码 + 单测；装入包）
[x] rebuild:native + 全量 electron:build（2026-08-09 21:35；本机工具链后缺失，后续包跳过）
[x] 阶段 N：服务端 reference / parse / merge / reset / export API（源码）
[x] 阶段 N：Settings 新格式 UI + locale/search（源码）
[x] 阶段 N：主 UI Export + Electron 写文件 IPC（源码）
[x] 阶段 N：服务端自动化冒烟（导入/限额/Reset/导出/取消 parse；14/14）
[x] Wiki 主 UI 树形目录（源码 + 单测；装入 17:15 包）
[ ] OpenWiki 完整中文正文 Generate/Update 人工确认（建议非 free 模型）
[ ] 阶段 N：GUI 冒烟（拖放导入 / 真实模型解析合并 / Export Word 写盘）
